# Railway frontend service variables
# Root Directory: /
# Build Command: npm install && npm run build
# Start Command: npm run preview -- --host 0.0.0.0 --port $PORT

VITE_API_URL=https://YOUR-NEKO-API.up.railway.app/api
